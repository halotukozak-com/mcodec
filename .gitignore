
# GSD planning docs (local-only)
.planning/

# Mill build artifacts
out/
.bsp/
.scoverage/

# scala-cli build artifacts (still used by the compile-time benchmark sweep,
# which shells out to scala-cli per-library for an apples-to-apples measure)
.scala-build/
scala-doc

# benchmark run artifacts — keep the tidy CSVs, drop the bulky raw output
benchmark/results/*.json
benchmark/results/compile-raw.csv
benchmark/results-env.txt
__pycache__/
